spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USR}
    password: ${DB_PAS}

  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8080
